
Command
    gpt review diff : Review a git diff from file  Args:     diff (str, optional): The diff to
    review.
        Defaults to ".diff".     config (str, optional): The config to use. Defaults to
        "config.summary.yml".  Returns:     Dict[str, str]: The response from GPT-4.
        WARNING: Command group 'review' is in preview. It may be changed/removed in a future
        release.

Arguments
    --config           : Default: config.summary.yml.
    --diff             : Default: .diff.

Global Arguments
    --debug            : Increase logging verbosity to show all debug logs.
    --help -h          : Show this help message and exit.
    --only-show-errors : Only show errors, suppressing warnings.
    --output -o        : Output format.  Allowed values: json, jsonc, none, table, tsv, yaml, yamlc.
                         Default: json.
    --query            : JMESPath query string. See http://jmespath.org/ for more information and
                         examples.
    --verbose          : Increase logging verbosity. Use --debug for full debug logs.

